apiVersion: v1
kind: Service
metadata:
  name: bildur-svc
  namespace: bildur
spec:
  selector:
    app: bildur
  type: NodePort
  ports:
    - protocol: TCP
      name: api-port
      port: {{ .Values.gateway.port }}
      targetPort: {{ .Values.gateway.port }}
